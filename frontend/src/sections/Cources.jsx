import Slider from "../components/Slider";
import TitleHeader from "../components/TitleHeader";

const Cources = () => {
  return (
    <section id="cources" className="section-padding">
      <TitleHeader title={"Cources"} sub={"Courses"} />
      <Slider />
    </section>
  );
};

export default Cources;
